/*package com.cinemaeBooking.repository;

import java.util.Collection;

import org.springframework.data.repository.CrudRepository;

import com.cinemaeBooking.entities.PayCard;
import com.cinemaeBooking.entities.User;


public interface PayCardRepository extends CrudRepository<PayCard, Long> {
	
	PayCard findById(int id);
	PayCard findByUser(User user);
	Collection<PayCard> finAllByUser(User user);

}
*/